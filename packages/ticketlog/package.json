{
  "name": "@ticketlog/ticketlog",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/**/*.test.js",
    "validate:api": "node dist/runApiValidation.js",
    "validate:browser": "node dist/browser/runValidation.js"
  },
  "dependencies": {
    "@playwright/test": "^1.46.1",
    "@ticketlog/domain": "*"
  }
}
